# Brevo Email Configuration
# Choose either Brevo API or Brevo SMTP (API is recommended)

# Option 1: Brevo API (Recommended)
# Get your API key from https://app.brevo.com/settings/keys/api
BREVO_API_KEY=xkeysib-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option 2: Brevo SMTP (Alternative)
# Get SMTP credentials from https://app.brevo.com/settings/smtp-and-api
BREVO_SMTP_LOGIN=7c7e60002@smtp-brevo.com
BREVO_SMTP_PASSWORD=your-smtp-password

# Production - From Email (must be verified in Brevo)
BREVO_FROM_EMAIL=noreply@yourdomain.com

# Development - Gmail credentials (fallback when Brevo is not configured)
GOOGLE_EMAIL=your-gmail@gmail.com
GOOGLE_PASSWORD=your-app-password

# Environment
NODE_ENV=production

# Frontend URL for email links
FRONTEND_URL=https://your-frontend-domain.com

# Priority Order:
# 1. Brevo API (if BREVO_API_KEY is set)
# 2. Brevo SMTP (if BREVO_SMTP_LOGIN and BREVO_SMTP_PASSWORD are set)
# 3. Gmail SMTP (fallback for development)
